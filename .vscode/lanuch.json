{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Reticulum Module",
            "type": "python",
            "request": "launch",
            "module": "RNS.Reticulum",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}